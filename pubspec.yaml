name: ct312hm01_temp
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_generative_ai: ^0.4.6
  provider: ^6.1.2
  hive_flutter: ^1.0.0
  shared_preferences: ^2.5.2
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    #using this instead of .env for website compatibility
    - lib/dotenv
    - assets/avatar/logo.png

